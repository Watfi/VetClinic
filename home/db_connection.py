"""Deprecated: kept only so old imports do not crash at import time.

The desktop edition stores all data in the local SQLite database via the
Django ORM (see ``home.models``). New code must import models directly.
"""
